# Nixpacks configuration for Railway deployment
# This installs FFmpeg which is required for audio streaming

[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = ["cd discord-music-bot && pip install -r requirements.txt"]

[start]
cmd = "cd discord-music-bot && python bot.py"
